# User-effort budget

Every step between intent and shared audio spends customer effort.

The default path may use one app open, one person or Open action, and at most one permission decision. Track selection, privacy education, discovery calibration, and provider diagnostics should not appear unless the current context genuinely requires them.

When a new step is proposed, measure whether it improves join success enough to justify the additional time and abandonment.
