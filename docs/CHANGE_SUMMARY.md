# Change summary

The customer-worship overhaul replaces the five-product feature showcase with a focused shared-listening utility, rebuilds the browser demo, hardens authentication and WebSockets, minimizes location data, removes discovery contact leakage and fake compatibility precision, ungates core access, adds typed telemetry, tests trust boundaries, and aligns repository claims with committed code.
