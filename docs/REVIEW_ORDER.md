# Suggested review order

1. `demo/index.html`, `demo/styles.css`, and `demo/app.js` for the product redesign.
2. `backend/services/presence.py` and `backend/routes/discovery.py` for privacy behavior.
3. `backend/routes/auth.py`, `backend/main.py`, and `backend/config.py` for trust boundaries.
4. `backend/routes/telemetry.py` and `docs/EVENTS.md` for observability.
5. `.github/workflows/ci.yml` and `backend/tests/` for regression coverage.
6. `README.md` and `docs/` for repository truth and operating direction.
